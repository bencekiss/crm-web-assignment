<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../styles.css">

    <title>Contacts</title>
  </head>
  <body>
    <h1>All contacts</h1>
    <table>
      <tbody>
        <% Contact.all.each do |contact| %>
          <tr><td><%= contact.first_name %></td> <td><%= contact.last_name %></td> </tr>
          <% end %>
      </tbody>
    </table>

  </body>
</html>
